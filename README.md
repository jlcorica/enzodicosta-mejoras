# Enzo Di Costa — Staging / Ensayo

Repositorio público de **pruebas** para revisar cambios visuales del sitio enzodicosta.com antes de publicarlos en producción.

- Vista en vivo: se activa GitHub Pages sobre `main` — link abajo.
- Este repo se actualiza automáticamente cada vez que se hace un commit en el repositorio de trabajo local ("Website").
- **No es el repo de producción.** El repo real es `jlcorica/enzodicosta` (privado), que a su vez alimenta el deploy en el servidor. `sergioanselmi/enzodicosta` es el repo histórico/original, con el que también hay que coordinar.
